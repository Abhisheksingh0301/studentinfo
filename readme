pythonanywhere = The free platform to host python page with sqlite

# Run in virtual environment : .\venv\Scripts\activate      

#"build:css": "tailwindcss -i ./tailwind/input.css -o ./static/output.css  --watch"

requirements.txt file is same as package.json. This command generates the same file.
#pip freeze > requirements.txt

# pip install -r requirements.txt => To install all the dependancy from this file.

# To generate requirements.txt file => pip freeze > requirements.txt

# To execute the program in background => pythonw.exe run.py
